## Summary

<!-- What does this PR change and why? -->

## Changes

-

## Checklist

- [ ] `ruff check` and `ruff format --check` pass locally
- [ ] `pytest` passes locally
- [ ] Docs updated (README / SKILL.md / plan documents where relevant)
- [ ] No secrets or machine-specific absolute paths committed
- [ ] No vendored upstream content committed (`oss/`, `sessions/`, `models/`, `.venv/` stay ignored)
- [ ] Any change to a script's documented JSON schema is flagged as a **breaking change**
      with a schema version bump in both the docstring and dependent skills
